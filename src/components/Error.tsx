import { Text } from "@chakra-ui/react";

const Error = () => {
  return (
    <Text mt={16} fontSize={["xl", "2xl", "3xl"]}>
      An error occured while loading the data
    </Text>
  );
};

export default Error;
